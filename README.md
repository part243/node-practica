# Notas.
eSTE ES MI PRIMERO PROGRAMA
```
Options:
      --help                  Show help                                [boolean]
      --version               Show version number                      [boolean]
  -b, --base, --bs            Es la base de la tabla de multiplicar
                                                             [number] [required]
  -h, --hasta, --limit, --li  hasta que número va a llegar la tabla     [number]
  -l, --lista                 true para mostrar la tabla y false para no mostrar
                                                      [boolean] [default: false]
```